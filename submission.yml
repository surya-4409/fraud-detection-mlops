Setup: pip install "setuptools<70.0.0" wheel && pip install -r requirements.txt
train: 'python src/prepare_data.py && python src/train.py'
start-api: 'docker build -t fraud-detection-api:latest . && docker run -d -p 8000:8000 fraud-detection-api:latest'
test-api: 'pytest tests/test_api.py -v -s'